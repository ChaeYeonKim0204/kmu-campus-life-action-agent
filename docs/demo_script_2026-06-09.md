# 발표 데모 스크립트 — 졸업사정 컨설팅 에이전트 (2026-06-09, ≤15분)

> 스크립트 낭독 금지 → 아래는 **비트(말할 포인트) 단위**. 데모는 S4 → S1 순서 2명.
> 사전 준비: 백엔드 :8001 가동(`uvicorn app:app --port 8001`), 프론트(:5173 또는 dist),
> **해설 캐시 warm-up 완료 상태**(데모 4명 모두 — 동일 입력은 캐시라 지연 0·같은 출력),
> **상담(what-if) 캐시 warm-up**: `/home/carol/exit/envs/kmu-agent/bin/python scripts/warm_whatif_cache.py`
> (⚠️ 반드시 conda kmu-agent 3.11 — 시스템 python 3.8은 import 즉사) 1회 실행 후
> 출력의 환각 검수 라인 확인 — 이후 칩 질문은 오프라인 동작.
> **발표 전 체크**: S4·S1 보고서에서 🤖 총평 카드가 뜨고 펼치면 시나리오 비교표·'검토 후 제외'가 보이는지 확인. **같은 스크립트가 에이전트 총평
> 캐시(`summary_cache.json`)도 함께 워밍업**(학생별 채택 시나리오·다양성 검수 표 출력).
> ⚠️ **데이터(요건·카탈로그) 정정 시 `whatif_cache.json`+`summary_cache.json` 둘 다 삭제 후
> 재워밍업** — 총평 캐시 키는 진단 수치 전체라 정정에 민감(의도된 동작). 질문에 없는 융합 변경은
> 서버의 환각 가드가 결정론으로 무시하고 카드에 "환각 가드" 가정을 표시함.

## 타임라인 (15:00)

| 시간 | 비트 |
|---|---|
| 0:00–1:30 | **문제** — "졸업요건은 학번·전공마다 다르고, '나 뭘 더 들어야 졸업?'은 막막하다. 사람이 하면 요람 수십 페이지 대조 작업" (Work Smarter 프레임) |
| 1:30–3:00 | **vs ChatGPT 1장** — 같은 질문에 ChatGPT: 일반론+학점 환각. 우리: 내 수강내역×내 학번 요람×학사규정을 **정확히 계산**. "중복인정 12학점 캡, 직전 3.75↑ +3학점 — 이런 건 흉내 못 냄" |
| 3:00–9:00 | **라이브 데모 6분** (아래 비트) |
| 9:00–12:00 | **아키텍처** — **첫 문장(교수 피드백 'workflow 독특한 거 하나'의 답): "저희 워크플로우는 두 종류입니다 — 졸업 가능 여부를 계산하는 판정형 RAG 워크플로우, 그리고 '휴학하면? 부전공 빼면?'을 다시 계산하는 시뮬레이션형 워크플로우"** → 인라인 워크플로우 그래프 리플레이: 노드 분절(verify 3 + audit 8 + 상담 7) → **분기 갈래**(통과 vs 미배치→초과학기 / 조건 가드 vs 안내 종료) → **"LLM은 파란 노드 넷, 전부 판정 금지"**: ① 요람 RAG 해설 = 규정 chunk로 *근거 생성*(RAG), ② 상담 매개변수 추출기 = 자연어→*도구 파라미터*(Tool Calling, 예제06 패턴), ③·④ **에이전트 총평 = 단일 턴 ReAct**(Thought: LLM이 갈림길 후보·값 선정 → Action: 결정론 재실행 → Observation: diff·탈락 기록 → Answer: 검증된 총평) — **교수님의 'ReAct를 풀어야 함' 피드백에 대한 답**. **"졸업 가능 여부는 어느 LLM도 답하지 않습니다 — 판정·로드맵·비교는 전부 결정론 도구가 계산"** → 해설 검증(validator)·조건 가드(IF/ELSE)가 환각·범위 밖 입력을 잡는 것까지 |
| 12:00–12:30 | **클로징 30초** — "정리하면, 단순한 챗봇이 아닙니다. ① 내 수강내역×학번별 요람×학사규정을 결정론으로 계산하고(중복인정 12 캡·제32조), ② LLM은 파란 노드 넷(해설·질문 해석·갈림길 선정·총평)에서만 — **졸업 판정은 절대 LLM이 하지 않습니다**, ③ 결과는 텍스트가 아니라 컨설팅 보고서 — 뭐가 부족하고 왜 부족하고 어떻게 채울지, 휴학·부전공 변경 시 어떻게 바뀌는지까지 같은 엔진으로 재시뮬레이션합니다. 근거와 계산이 분리된 졸업사정 컨설팅 에이전트입니다." |
| 12:30–15:00 | **Q&A** (예상 질문 카드 아래) |

## 데모 비트 (6분)

**S4 최계절 (3분) — happy path + 학번별 요람:**
1. (0:30) 학생정보 입력: 입학 **2022**·AI빅데이터융합경영 + 데이터사이언스 다전공, 현재 2026-1·잔여 2, **계절 허용 + 직전학기 3.75↑ 체크** → .xls **7개** 업로드
2. (0:45) 검증 테이블(HITL): 재수강 의심 팝업 1건(경영통계 — 화면에 뜨는 과목명 그대로 읽기) → "성적표에 성적이 없으니 사람이 확정 — 데이터 신뢰의 다리" → 확정
3. (1:00) 졸업사정 실행 → 보고서: **적용 요람 "2022 요람(학번 2022 기준)"** — 학번별 요람이 화면에서 작동(기초교양 최저 7 — 글로벌영어 해지 일괄 반영). 미이수 필수 **딥러닝 1과목**(2025 요람 기준이면 유레카도 필수지만 2022학번이라 제외 — 연도별 필수 차이) → 로드맵이 딥러닝+융합 잔여를 2학기에 배치, **feasible·A+(여유)**. 멘트: "등급은 졸업 여유도입니다 — A+는 계절학기 없이 학기당 15학점 이하로도 졸업 가능하다는 뜻. 절대 부족량이 아니라 시나리오 시뮬레이션으로 판정합니다". 계절·성적우수(+3)는 입력 모델로 지원(이 학생은 불필요)
3-1. (0:25) **🤖 에이전트 총평 카드** (판정 배지 바로 아래 — 교수③ 처방: 라이브에 박을 것):
   "보고서가 나오는 순간, 에이전트가 **스스로 이 학생의 갈림길 후보 5개를 골라** 결정론 시뮬레이터로 전부 돌려봤습니다" → 카드 펼치기 → 시나리오 비교 미니표 + **'검토 후 제외' 목록**("효과 없음·전제 불일치까지 탈락 사유를 공개합니다 — 에이전트가 진짜 탐색했다는 증거"). 그래프의 파란 노드(갈림길 선정) 손가락으로 짚기
4. (0:30) **규정 근거 해설** 섹션: 요람 원문 인용(Y 배지 — 페이지는 화면에 뜨는 값을 그대로 읽을 것) 달린 해설 — "보고서가 스스로 규정을 설명, 챗봇 아님". 필수과목 해설은 초록 **'결정론' 칩**("필수는 검색이 아니라 교과과정표 데이터로 확정 — 정책 해설만 RAG"). '미확인' 줄은 현 캐시에선 0건(뜨면 "검증기가 근거 약한 문장을 잡은 것"으로 프레이밍)
5. (0:45) **🔮 졸업 시나리오 상담(what-if)** — 칩 "다음 학기 휴학하면?" 1클릭: "보고서를 받은 학생의 다음 질문은 늘 '그럼 ~하면요?'입니다. LLM이 질문을 시뮬레이션 파라미터로 바꾸고(매개변수 추출기 — 수업 예제06 패턴), **판정은 방금 그 결정론 엔진이 다시 계산**" → before/after 카드(예상 졸업 지연·리스크 유지)와 그래프에 상담 클러스터 점등. "ChatGPT는 이 질문에 일반론을 말하지만, 우리는 이 학생의 남은 학기로 다시 계산합니다"

**S1 김융합 (2:45) — 단일 최강 장면(중복인정 캡):**
1. (0:25) 졸업반·잔여 1학기, 같은 다전공 → 실행
2. (1:00) "겹침 **27학점**인데 **중복인정은 12까지만** — 초과 **15학점**은 제1전공 또는 융합 한쪽만 인정. ChatGPT는 이 캡을 몰라 '졸업 가능'이라 합니다. 우리는 그 결과 **융합 9학점 부족**까지 계산하고, 로드맵이 그 9학점을 **잔여 학기(2026-2)에 실제 개설되는 과목만 골라**(다변량·비즈니스통계응용·인공지능) 정확히 배치 → **feasible·A+**. 1학기-전용 과목을 골랐다면 초과학기가 났을 텐데, 플래너가 개설학기까지 보고 선택합니다" (3-way 배정 UI 한 번 클릭) ※ 수치(27/12/15/9)는 리허설 실측 — 화면에 뜨는 값을 그대로 읽을 것
3. (0:40) 워크플로우 그래프(인라인): S1도 통과 갈래(전부 초록). **분기 갈래(⑦′ 초과학기) 점등은 S3로 10초 추가 시연**(2학년·필수 8건·D·초과 +4 — 진짜 blocked) — "학생마다 다른 경로가 켜집니다"(분기 시각화 채점 포인트)
4. (0:40) 근거 토글: G1~G5 + Y(요람 RAG chunk — 페이지는 **화면 표시값만** 읽기, G1엔 페이지 없음) — "모든 판정에 출처"

## Fallback (실패 대비)

| 장애 | 증상 | 대응 |
|---|---|---|
| OpenAI 다운/지연 | 해설 섹션에 "생성 불가 — 결정론 진단·G 근거는 유효" | **데모 계속 진행** — 캐시 덕에 사전 warm-up된 4명은 키 없이도 해설 나옴. 멘트: "해설만 degrade, 판정은 무영향 — 이게 결정론 본체의 가치" |
| 백엔드 죽음 | 화면 에러 | `lsof -ti:8001 \| xargs kill -9; uvicorn app:app --port 8001` (10초). 보험: 발표 전 S4·S1 결과 화면 풀스크린 캡처 |
| **코드 수정 후 미재기동** | 구버전 응답(예: 가드 미적용) | **코드를 1줄이라도 고쳤으면 무조건 kill→재기동** — `--reload` 없는 프로세스는 stale 코드를 계속 서빙(WSL2 좀비 패턴, 리허설에서 실제 재현). in-process 테스트 통과 ≠ 라이브 통과 |
| 프론트 죽음 | 5173 접속 불가 | `frontend/dist` 빌드본을 :8001이 서빙(`http://127.0.0.1:8001/`) — Vite 없이 동작 |
| 와이파이 없음 | LLM·신규 해설 불가 | 캐시로 데모 4명 전원 해설 포함 정상 동작(완전 오프라인 OK — Chroma·카탈로그 로컬) |
| 상담 질문 캐시 미스 | "LLM 미설정/해석 실패" 회색 카드 또는 ~4초 지연 | **칩 질문만 사용**(warm-up으로 사전 캐시됨). 심사자가 직접 타이핑하면(라이브 검증 1순위 리스크): 온라인 시 "처음 보는 질문은 LLM 해석 ~4초 — 해석만 LLM" 멘트로 침묵을 설명으로 전환, 오프라인 시 회색카드 안내문 그대로 읽기. 멘트: "처음 보는 질문은 LLM 해석이 필요하고, 해석만 LLM — 판정은 항상 결정론" |
| **총평 캐시 미스**(테이블 체크박스·이수구분 편집 후 재사정 / 새 학생) | 온라인: LLM 2~4콜 ~10초+ 지연 · 오프라인: "LLM 미설정" 회색 총평 카드(본체 무영향) | **데모 중 검증 테이블 편집 후 재사정 금지**(재수강 팝업 확정은 워밍업에 포함돼 안전). 편집 시연이 필요하면 "총평은 새 진단에 맞춰 재생성됩니다(라이브 ~10초)" 멘트로 침묵을 설명으로 전환 |
| 시간 초과 | — | **생략 순서**: S1 그래프 비트(3번) → S4 상담 칩 비트(5번) → 근거 토글 상세(S1 4번). **절대 생략 금지**: S4 총평 카드(3-1)·S1 중복인정 캡 장면·아키텍처 파란 노드 4개(교수③ 심사 기준) |

**변주 안전**: 데모 학생의 수강 파일 마지막 학기와 현재 학기 사이 공백은 **휴학 복학생 서사**(질문 시 그렇게 답변 — demo_students/README 참고). ai_bigdata는 2022~2025 전 학번 연도별 요람 데이터 보유(학과 공식 시트 기준) — 학번 바꿔달라는 요청에도 안전. 단 2021 이하는 학과 신설(2022) 이전이라 "해당 학과 학번이 존재하지 않는다"고 답하면 됨.

## 예상 Q&A 카드

0. **"등급 기준이 뭔가?"** → "졸업 여유도 사다리입니다 — S(요건 충족·조기졸업 안내), A+(계절 없이 15학점 이하), A(계절 없이 법정 상한), B(계절 필요), C(검증된 초과 1학기), D(그 이상). 전부 결정론 플래너 시나리오 시뮬레이션으로 판정하고, 점수는 부족 요인 가중치 합산(별도 지표)입니다."
1. **"이게 에이전트인가, 계산기 아닌가?"** → "자유 LLM이 답을 쓰는 게 아니라, 검증된 데이터·요람을 도구 노드들이 처리하고 분기(HITL/blocked/초과학기/해설 검증)를 타는 bounded agent입니다. LLM은 요람 근거 해설에 제한 사용 — 환각이 위험한 계산엔 안 씁니다."
2. **"생성AI는 어디에?"** → 그래프 파란 노드 넷(해설·상담 추출기·갈림길 선정·총평). 특히 ③갈림길 선정: "에이전트가 보고서를 보고 이 학생의 갈림길을 스스로 골라 시뮬레이션을 돌려보고 조언 — 탈락 후보까지 사유와 함께 화면에 남습니다(검토 후 제외 토글)". "① RAG 해설 — 요람 chunk만 근거로 해설을 생성하고 validator가 인용을 검증(근거 없는 줄은 '요람 원문에서 직접 확인되지 않음'). ② 상담 매개변수 추출기 — 자연어 질문을 안전한 시뮬레이션 파라미터로 변환(Tool Calling). **졸업 판정은 RAG도 추출기도 하지 않습니다 — 검증된 졸업사정 도구가 다시 계산합니다.**"
2-1. **"왜 LLM을 더 안 썼나?"** → "교수님이 강조하신 출력 일관성 때문에 의도된 설계입니다. LLM은 비정형(자연어·규정 원문)을 다루는 두 자리에만 쓰고, 정형 계산은 전부 결정론 — HyDE 슬라이드의 '사람들은 약관 용어를 쓰지 않는다' 문제도, 검색 쿼리를 학생 자연어가 아닌 결정론 노드가 요람 용어로 생성해 구조적으로 회피했습니다."
3. **"ChatGPT에 성적표 붙여넣으면?"** → 코드 매칭·학번별 요람·중복인정 캡·제32조 상한·재수강 제외를 일관 계산 못 함 + 매번 답이 다름. 우리는 같은 입력=같은 결과(시연 가능).
4. **"2023학번 넣으면?"** → ai_bigdata도 2022~2025 학번별 요람 실적용(필수 11↔12과목·유레카 추가 여부가 연도별로 다름 — S3은 '2024 요람(학번 2024 기준)', S4를 2025학번으로 바꾸면 유레카 필수 추가가 화면에 반영됨. 기초 7·일선 58은 글로벌영어 해지 일괄 적용으로 전 학번 동일). 미래모빌리티도 2023 요람 동작.
4-1. **"플래너가 과목을 잘못 골라 초과학기로 오판할 수 있지 않나요?"** (실제 있었던 결함 — 수정 완료 서사로 답변) → "정확히 그 버그가 있었습니다. 후보 선택이 개설학기를 안 봐서 S1에게 1학기-전용 과목을 고집해 거짓 '초과 1학기'를 냈고, 규칙 위반만 찾는 적대 검증으론 못 잡다가 **도메인 반증 리뷰**('더 나은 계획이 존재하는가')로 발견해 수정했습니다(2026-06-05). 지금은 잔여 학기에 개설되는 후보를 우선 선택하고, 데모 4명의 손으로 반증 검증한 ground truth를 회귀 테스트로 고정했습니다 — 결정론도 일관되게 틀릴 수 있다는 게 교훈이라, soundness 검증에 optimality 검증을 추가한 겁니다."
5. **"왜 로드맵을 LLM이 안 짜나?"** → "교수님이 강조하신 출력 일관성 — 배치는 제약충족 문제라 결정론이 정답. LLM 시안은 같은 입력에 다른 답을 냈습니다. 적대 검증 13라운드로 결정론 엔진을 다졌습니다."

## 개인 기여 보고서용 기능–파일–화면 매핑

| 기능 | 핵심 파일 | 테스트 | 발표에서 보이는 화면 |
|---|---|---|---|
| 엑셀 파싱·코드 매칭 | `graduation_center/v2/excel_parser.py`, `catalog.py` | `test_v2_pipeline` | 업로드→검증 테이블 |
| HITL 검증(재수강·폐강) | `verification.py` | 〃 | 재수강 팝업·테이블 토글 |
| 졸업사정(학번별 요람·융합 중복인정) | `audit_v2.py`, `requirements_by_year.json`, `required_names_by_year.json` | `test_v2_pipeline`, `test_graduation_*` | 영역 게이지·융합 블록·3-way |
| 결정론 로드맵+검증+초과학기 | `planner.py` | 〃 | 로드맵 타임라인·초과학기 카드 |
| 리스크 등급·완화 게이트 | `risk.py` | 〃 | 종합 판정 배지 |
| 규정 근거 해설(RAG+validator) | `explain.py` | `test_v2_explain` (7종) | 📖 해설 섹션·Y 인용 |
| citation(G1~G5) | `pipeline.py:_build_sources` | — | 근거 토글 |
| 워크플로우 그래프(분기 갈래) | `frontend/WorkflowGraph.jsx`, trace는 `pipeline.py` | — | 인라인 그래프·#workflow |
| 졸업 시나리오 상담 Agent(what-if) | `whatif.py` (매개변수 추출기·조건 가드·diff·다음행동), `/graduation/v2/whatif` | `test_v2_whatif` (36종) | 🔮 상담 카드·그래프 상담 클러스터 |
| 데모 학생 생성기 | `scripts/make_demo_students.py` | round-trip 검증 | 업로드 파일 4세트 |
| 상담 캐시 워밍업 | `scripts/warm_whatif_cache.py` (환각 검수 포함) | — | (발표 전 1회 실행) |
| 품질 캠페인 | `docs/reviews/review_round*.md` (적대 5R + 수렴 7R + 교수평가 6인 + 반증 리뷰) | 캠페인 당시 236 → 정리 후 현행 138(129 passed) | — |
