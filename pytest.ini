[pytest]
testpaths = tests
norecursedirs = unused frontend data .git
# Test markers
markers =
    live_llm: tests that make real OpenAI API calls (require API key + incur cost). Skipped by default; opt-in via `pytest -m live_llm`.
